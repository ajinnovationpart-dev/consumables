# 📱 화면별 상세 스펙 (Screen Specifications)

## 📋 목차
- [1. 로그인 페이지](#1-로그인-페이지)
- [2. 사용자 대시보드](#2-사용자-대시보드)
- [3. 관리자 대시보드](#3-관리자-대시보드)
- [4. 신청 등록 페이지](#4-신청-등록-페이지)
- [5. 내 신청 목록](#5-내-신청-목록)
- [6. 전체 신청 목록 (관리자)](#6-전체-신청-목록-관리자)
- [7. 통계 및 리포트 (관리자)](#7-통계-및-리포트-관리자)
- [8. 신청 상세 페이지 (사용자)](#8-신청-상세-페이지-사용자)
- [9. 신청 상세 관리 (관리자)](#9-신청-상세-관리-관리자)
- [10. 내 정보 페이지](#10-내-정보-페이지)

---

## 1. 로그인 페이지

### 📍 경로
- **URL**: `/login`
- **권한**: 비로그인 사용자
- **리다이렉션**: 로그인 성공 시 역할별 대시보드로 이동

### 📊 필수 데이터

#### 입력 필드
| 필드명 | 타입 | 필수 | 유효성 검증 | 설명 |
|--------|------|------|------------|------|
| `userId` | `string` | ✅ | - 최소 1자<br>- 영문/숫자 조합 | 사용자 ID |
| `password` | `string` | ✅ | - 최소 1자 | 비밀번호 |

### 🔧 주요 기능

1. **로그인 처리**
   ```typescript
   POST /api/auth/login
   Body: { userId: string, password: string }
   Response: { success: boolean, token: string, user: User }
   ```

2. **세션 관리**
   - JWT 토큰 생성 및 저장
   - `localStorage` 또는 `sessionStorage`에 토큰 저장
   - 역할별 리다이렉션:
     - `관리자` → `/admin/dashboard`
     - `신청자` → `/dashboard`

3. **에러 처리**
   - 잘못된 ID/PW: "사용자 ID 또는 비밀번호가 올바르지 않습니다."
   - 네트워크 오류: "로그인 중 오류가 발생했습니다."

### 🎨 UI 컴포넌트
- 로그인 폼 (중앙 정렬)
- 로딩 스피너 (제출 중)
- 에러 메시지 표시

---

## 2. 사용자 대시보드

### 📍 경로
- **URL**: `/dashboard`
- **권한**: 로그인 사용자 (신청자)
- **리다이렉션**: 비로그인 시 `/login`

### 📊 필수 데이터

#### 표시 데이터
| 데이터 | 타입 | 설명 | API |
|--------|------|------|-----|
| `user.name` | `string` | 사용자 이름 | `GET /api/auth/me` |
| `user.team` | `string` | 소속팀 | `GET /api/auth/me` |
| `user.region` | `string` | 지역 | `GET /api/auth/me` |
| `stats.requested` | `number` | 신청 건수 | `GET /api/requests/stats` |
| `stats.inProgress` | `number` | 진행중 건수 | `GET /api/requests/stats` |
| `stats.completed` | `number` | 완료 건수 | `GET /api/requests/stats` |
| `stats.total` | `number` | 전체 건수 | `GET /api/requests/stats` |
| `recentRequests[]` | `Array<Request>` | 최근 신청 내역 (최대 5건) | `GET /api/requests?limit=5` |
| `notifications[]` | `Array<Notification>` | 알림 목록 | `GET /api/notifications` |

### 🔧 주요 기능

1. **통계 카드 표시**
   - 신청 건수 (전체)
   - 진행중 건수
   - 완료 건수
   - 완료율 계산: `(completed / total) * 100`

2. **최근 신청 내역**
   - 최대 5건 표시
   - 최신순 정렬
   - 클릭 시 상세 페이지로 이동

3. **빠른 액션**
   - "새 신청하기" 버튼 → `/requests/new`
   - "내 신청 목록" 버튼 → `/requests`
   - "내 정보" 버튼 → `/profile`

4. **알림 표시**
   - 미처리 알림 개수 표시
   - 알림 클릭 시 상세 페이지로 이동

### 🎨 UI 컴포넌트
- 통계 카드 (4개)
- 최근 신청 테이블
- 알림 목록
- 빠른 액션 버튼

### ⚡ 성능 최적화
- React Query 캐싱 (5분)
- 사용자 정보는 `localStorage` 캐싱
- 병렬 API 호출 (`Promise.all`)

---

## 3. 관리자 대시보드

### 📍 경로
- **URL**: `/admin/dashboard`
- **권한**: 관리자만 접근
- **리다이렉션**: 비관리자 → `/login`

### 📊 필수 데이터

#### 표시 데이터
| 데이터 | 타입 | 설명 | API |
|--------|------|------|-----|
| `user.name` | `string` | 관리자 이름 | `GET /api/auth/me` |
| `period.startDate` | `Date` | 조회 시작일 | 사용자 입력 |
| `period.endDate` | `Date` | 조회 종료일 | 사용자 입력 |
| `stats.new` | `number` | 신규 건수 | `GET /api/admin/stats?startDate&endDate` |
| `stats.requested` | `number` | 접수중 건수 | `GET /api/admin/stats?startDate&endDate` |
| `stats.inProgress` | `number` | 진행중 건수 | `GET /api/admin/stats?startDate&endDate` |
| `stats.delayed` | `number` | 지연 건수 | `GET /api/admin/stats?startDate&endDate` |
| `stats.completed` | `number` | 완료 건수 | `GET /api/admin/stats?startDate&endDate` |
| `stats.total` | `number` | 전체 건수 | `GET /api/admin/stats?startDate&endDate` |
| `urgentRequests[]` | `Array<Request>` | 긴급 처리 필요 (1일 이상 경과) | `GET /api/admin/urgent` |
| `delayedRequests[]` | `Array<Request>` | 지연 건 (3일 이상 경과) | `GET /api/admin/delayed` |

### 🔧 주요 기능

1. **기간 선택**
   - 시작일/종료일 입력
   - 빠른 선택: 오늘, 이번 주, 이번 달, 이번 분기
   - 기본값: 이번 달

2. **통계 카드**
   - 신규, 접수중, 진행중, 지연, 완료, 전체
   - 각 통계는 선택된 기간 기준

3. **긴급 처리 필요**
   - 상태가 "접수중"이고 신청일로부터 1일 이상 경과
   - 테이블 형식 표시
   - "처리" 버튼 클릭 시 상세 페이지로 이동

4. **지연 건 모니터링**
   - 상태가 "접수완료" 또는 "발주지연"이고 발주일로부터 3일 이상 경과
   - 지연 일수 표시
   - "상태 변경" 버튼

5. **네비게이션**
   - "전체 신청 목록" → `/admin/requests`
   - "통계 및 리포트" → `/admin/statistics`

### 🎨 UI 컴포넌트
- 기간 선택 필터
- 통계 카드 (6개)
- 긴급 처리 테이블
- 지연 건 테이블

### ⚡ 성능 최적화
- 클라이언트 측 필터링 (서버에서 전체 데이터 조회 후)
- React Query 캐싱
- 가상 스크롤링 (대량 데이터)

---

## 4. 신청 등록 페이지

### 📍 경로
- **URL**: `/requests/new`
- **권한**: 로그인 사용자
- **리다이렉션**: 비로그인 → `/login`

### 📊 필수 데이터

#### 입력 필드

##### 신청자 정보 (읽기 전용, 자동 입력)
| 필드명 | 타입 | 필수 | 설명 | 소스 |
|--------|------|------|------|------|
| `requesterName` | `string` | ✅ | 이름 | `user.name` |
| `requesterTeam` | `string` | ✅ | 소속팀 | `user.team` |
| `requesterRegion` | `string` | ✅ | 지역 | `user.region` |

##### 부품 정보
| 필드명 | 타입 | 필수 | 유효성 검증 | 설명 |
|--------|------|------|------------|------|
| `itemName` | `string` | ✅ | - 최소 1자<br>- 최대 200자 | 품명 (예: 연료필터) |
| `modelName` | `string` | ❌ | - 최대 100자 | 모델명 (예: HD-123) |
| `quantity` | `number` | ✅ | - 정수<br>- 최소 1<br>- 최대 9999 | 수량 |
| `assetNo` | `string` | ✅ | - 최소 1자<br>- 최대 50자 | 관리번호 (예: DS25C305) |
| `serialNo` | `string` | ❌ | - 최대 100자 | 시리얼번호 (예: SN-12345) |

##### 수령 정보
| 필드명 | 타입 | 필수 | 유효성 검증 | 설명 |
|--------|------|------|------------|------|
| `deliveryPlace` | `string` | ✅ | - 선택 목록 또는 "기타"<br>- "기타" 선택 시 `customDeliveryPlace` 필수 | 수령지 |
| `customDeliveryPlace` | `string` | 조건부 | - `deliveryPlace === "기타"`일 때 필수<br>- 최대 200자 | 기타 수령지 |
| `phone` | `string` | ❌ | - 형식: `01X-XXXX-XXXX`<br>- 정규식: `/^01[0-9]-[0-9]{4}-[0-9]{4}$/` | 전화번호 |
| `company` | `string` | ❌ | - 최대 100자 | 업체명 |

##### 기타
| 필드명 | 타입 | 필수 | 유효성 검증 | 설명 |
|--------|------|------|------------|------|
| `remarks` | `string` | ❌ | - 최대 500자 | 비고 |
| `photo` | `File` | ✅ | - 이미지 파일만<br>- 최대 5MB<br>- 형식: JPG, PNG, GIF | 사진 첨부 |

### 🔧 주요 기능

1. **폼 제출**
   ```typescript
   POST /api/requests
   Body: {
     itemName: string;
     modelName?: string;
     quantity: number;
     assetNo: string;
     serialNo?: string;
     deliveryPlace: string;
     phone?: string;
     company?: string;
     remarks?: string;
     photoBase64?: string; // Base64 인코딩
   }
   Response: {
     success: boolean;
     requestNo: string; // 생성된 신청번호 (예: 2601070001)
     message?: string;
   }
   ```

2. **사진 업로드**
   - 파일 선택 → Base64 인코딩
   - 미리보기 표시
   - 파일 크기 검증 (최대 5MB)
   - 이미지 형식 검증

3. **유효성 검증**
   - 클라이언트 측 실시간 검증
   - 서버 측 최종 검증
   - 에러 메시지 표시

4. **신청번호 생성**
   - 서버에서 자동 생성
   - 형식: `YYMMDD0001` (예: 2601070001)
   - 중복 방지

5. **성공 처리**
   - 성공 모달 표시
   - 신청번호 표시
   - "대시보드로 이동" 버튼

### 🎨 UI 컴포넌트
- 신청자 정보 카드 (읽기 전용)
- 부품 정보 입력 폼
- 수령 정보 입력 폼
- 사진 업로드 영역 (드래그 앤 드롭)
- 사진 미리보기
- 제출 버튼
- 취소 버튼

### ⚡ 성능 최적화
- 이미지 압축 (클라이언트 측)
- Base64 인코딩 최적화
- 폼 상태 관리 (React Hook Form)

---

## 5. 내 신청 목록

### 📍 경로
- **URL**: `/requests`
- **권한**: 로그인 사용자
- **리다이렉션**: 비로그인 → `/login`

### 📊 필수 데이터

#### 표시 데이터
| 데이터 | 타입 | 설명 | API |
|--------|------|------|-----|
| `requests[]` | `Array<Request>` | 내 신청 목록 | `GET /api/requests/my` |
| `stats.requested` | `number` | 접수중 건수 | 클라이언트 계산 |
| `stats.inProgress` | `number` | 진행중 건수 | 클라이언트 계산 |
| `stats.completed` | `number` | 완료 건수 | 클라이언트 계산 |
| `stats.total` | `number` | 전체 건수 | 클라이언트 계산 |

#### Request 객체 구조
```typescript
interface Request {
  id: string;
  requestNo: string; // 신청번호 (예: 2601070001)
  requestDate: Date; // 신청일시
  itemName: string; // 품명
  quantity: number; // 수량
  status: '접수중' | '접수완료' | '발주지연' | '발주완료' | '처리완료' | '접수취소';
  handler?: string; // 담당자 이름
  orderDate?: Date; // 발주일시
  expectedDeliveryDate?: Date; // 예상납기일
  receiptDate?: Date; // 수령확인일시
}
```

### 🔧 주요 기능

1. **필터링**
   - 상태별 필터: 전체, 접수중, 진행중, 완료, 취소
   - 날짜 범위 필터 (선택)
   - 실시간 필터링 (클라이언트 측)

2. **정렬**
   - 기본: 최신순 (신청일시 내림차순)
   - 옵션: 오래된순, 상태순

3. **테이블 표시**
   - 신청번호, 신청일시, 품명, 수량, 상태, 담당자, 액션
   - 상태별 뱃지 색상:
     - 접수중: 파란색
     - 진행중: 노란색
     - 완료: 초록색
     - 취소: 회색

4. **액션**
   - "상세 보기" 버튼 → `/requests/:requestNo`
   - "수령 확인" 버튼 (처리완료 상태일 때만)

5. **통계 표시**
   - 상단에 통계 카드 (접수중, 진행중, 완료, 전체)

### 🎨 UI 컴포넌트
- 필터 섹션
- 통계 카드 (4개)
- 신청 목록 테이블
- 페이지네이션 (선택)

### ⚡ 성능 최적화
- React Query 캐싱
- 가상 스크롤링 (대량 데이터)
- 클라이언트 측 필터링

---

## 6. 전체 신청 목록 (관리자)

### 📍 경로
- **URL**: `/admin/requests`
- **권한**: 관리자만 접근
- **리다이렉션**: 비관리자 → `/login`

### 📊 필수 데이터

#### 표시 데이터
| 데이터 | 타입 | 설명 | API |
|--------|------|------|-----|
| `requests[]` | `Array<Request>` | 전체 신청 목록 | `GET /api/admin/requests` |
| `codes.statuses[]` | `Array<string>` | 상태 코드 목록 | `GET /api/codes/statuses` |
| `codes.regions[]` | `Array<string>` | 지역 코드 목록 | `GET /api/codes/regions` |

#### Request 객체 구조 (확장)
```typescript
interface AdminRequest extends Request {
  requesterName: string; // 신청자 이름
  requesterEmail: string; // 신청자 이메일
  team: string; // 소속팀
  region: string; // 지역
  photoUrl?: string; // 사진 URL
  handlerRemarks?: string; // 담당자 비고
}
```

### 🔧 주요 기능

1. **필터링**
   - 상태별: 전체, 접수중, 접수완료, 처리완료, 접수취소
   - 지역별: 전체, 서울, 부산, 대구, ...
   - 날짜 범위: 시작일 ~ 종료일
   - 실시간 필터링

2. **일괄 처리**
   - 체크박스로 다중 선택
   - "전체 선택" 기능
   - 일괄 상태 변경
   - 일괄 담당자 배정

3. **테이블 표시**
   - 신청번호, 신청일시, 신청자, 지역, 품명, 수량, 관리번호, 담당자, 사진, 상태, 액션
   - 사진 미리보기 (클릭 시 확대)

4. **액션**
   - "상세" 버튼 → `/admin/requests/:requestNo`
   - "일괄 처리" 버튼

5. **검색** (선택)
   - 신청번호, 품명, 신청자명으로 검색

### 🎨 UI 컴포넌트
- 필터 섹션 (상태, 지역, 날짜)
- 일괄 선택 체크박스
- 신청 목록 테이블
- 페이지네이션

### ⚡ 성능 최적화
- 서버 측 페이지네이션
- 인덱싱된 쿼리
- React Query 무한 스크롤

---

## 7. 통계 및 리포트 (관리자)

### 📍 경로
- **URL**: `/admin/statistics`
- **권한**: 관리자만 접근
- **리다이렉션**: 비관리자 → `/login`

### 📊 필수 데이터

#### 표시 데이터
| 데이터 | 타입 | 설명 | API |
|--------|------|------|-----|
| `period.startDate` | `Date` | 조회 시작일 | 사용자 입력 |
| `period.endDate` | `Date` | 조회 종료일 | 사용자 입력 |
| `stats.total` | `number` | 총 신청 건수 | `GET /api/admin/statistics?startDate&endDate` |
| `stats.completed` | `number` | 처리 완료 건수 | `GET /api/admin/statistics?startDate&endDate` |
| `stats.inProgress` | `number` | 진행 중 건수 | `GET /api/admin/statistics?startDate&endDate` |
| `stats.avgDays` | `number` | 평균 처리일 | 계산: `(완료 건들의 처리일 합) / 완료 건수` |
| `byStatus[]` | `Array<{name: string, value: number}>` | 상태별 분포 | 클라이언트 계산 |
| `byRegion[]` | `Array<{name: string, value: number}>` | 지역별 분포 | 클라이언트 계산 |
| `byDate[]` | `Array<{date: string, count: number}>` | 일별 추이 | 클라이언트 계산 |

### 🔧 주요 기능

1. **기간 선택**
   - 시작일/종료일 입력
   - 빠른 선택: 오늘, 이번 주, 이번 달, 이번 분기, 올해
   - 기본값: 최근 30일

2. **통계 카드**
   - 총 신청 건수
   - 처리 완료 (건수 및 비율)
   - 진행 중 (건수 및 비율)
   - 평균 처리일 (일)

3. **차트 표시**
   - **도넛 차트**: 상태별 분포
     - 접수중, 진행중, 완료, 취소
   - **막대 차트**: 지역별 통계
     - 서울, 부산, 대구, ...
   - **선형 차트**: 일별 추이
     - X축: 날짜, Y축: 신청 건수

4. **데이터 내보내기**
   - Excel 내보내기 (선택)
   - PDF 리포트 생성 (선택)

5. **조회**
   - "조회" 버튼 클릭 시 데이터 갱신

### 🎨 UI 컴포넌트
- 기간 선택 필터
- 통계 카드 (4개)
- 차트 영역 (3개 차트)
- 데이터 테이블 (선택)

### ⚡ 성능 최적화
- 클라이언트 측 집계 (서버에서 전체 데이터 조회 후)
- 차트 렌더링 최적화 (Recharts)
- 데이터 캐싱

---

## 8. 신청 상세 페이지 (사용자)

### 📍 경로
- **URL**: `/requests/:requestNo`
- **권한**: 로그인 사용자 (본인 신청만)
- **리다이렉션**: 비로그인 → `/login`, 타인 신청 → `/requests`

### 📊 필수 데이터

#### 표시 데이터
| 데이터 | 타입 | 설명 | API |
|--------|------|------|-----|
| `request.id` | `string` | 신청 ID | `GET /api/requests/:requestNo` |
| `request.requestNo` | `string` | 신청번호 | `GET /api/requests/:requestNo` |
| `request.requestDate` | `Date` | 신청일시 | `GET /api/requests/:requestNo` |
| `request.itemName` | `string` | 품명 | `GET /api/requests/:requestNo` |
| `request.modelName` | `string` | 모델명 | `GET /api/requests/:requestNo` |
| `request.quantity` | `number` | 수량 | `GET /api/requests/:requestNo` |
| `request.assetNo` | `string` | 관리번호 | `GET /api/requests/:requestNo` |
| `request.serialNo` | `string` | 시리얼번호 | `GET /api/requests/:requestNo` |
| `request.deliveryPlace` | `string` | 수령지 | `GET /api/requests/:requestNo` |
| `request.phone` | `string` | 전화번호 | `GET /api/requests/:requestNo` |
| `request.company` | `string` | 업체명 | `GET /api/requests/:requestNo` |
| `request.remarks` | `string` | 비고 | `GET /api/requests/:requestNo` |
| `request.photoUrl` | `string` | 사진 URL | `GET /api/requests/:requestNo` |
| `request.status` | `string` | 상태 | `GET /api/requests/:requestNo` |
| `request.handler` | `string` | 담당자 | `GET /api/requests/:requestNo` |
| `request.orderDate` | `Date` | 발주일시 | `GET /api/requests/:requestNo` |
| `request.expectedDeliveryDate` | `Date` | 예상납기일 | `GET /api/requests/:requestNo` |
| `request.receiptDate` | `Date` | 수령확인일시 | `GET /api/requests/:requestNo` |

### 🔧 주요 기능

1. **상세 정보 표시**
   - 신청 정보 (읽기 전용)
   - 처리 현황 (상태 진행바)
   - 사진 표시

2. **상태 진행바**
   ```
   접수중 → 접수완료 → 발주완료 → 처리완료
   ```
   - 현재 상태까지 하이라이트

3. **수령 확인**
   - 상태가 "발주완료"일 때만 표시
   - "수령 확인" 버튼 클릭
   ```typescript
   POST /api/requests/:requestNo/receipt
   Response: { success: boolean }
   ```
   - 성공 시 상태가 "처리완료"로 변경

4. **네비게이션**
   - "목록으로" 버튼 → `/requests`

### 🎨 UI 컴포넌트
- 신청 정보 카드
- 상태 진행바
- 사진 미리보기
- 수령 확인 버튼 (조건부)
- 목록으로 버튼

---

## 9. 신청 상세 관리 (관리자)

### 📍 경로
- **URL**: `/admin/requests/:requestNo`
- **권한**: 관리자만 접근
- **리다이렉션**: 비관리자 → `/login`

### 📊 필수 데이터

#### 표시 데이터
- [8. 신청 상세 페이지](#8-신청-상세-페이지-사용자)와 동일
- 추가: `handlerRemarks` (담당자 비고)

#### 입력 필드 (수정 가능)
| 필드명 | 타입 | 필수 | 설명 |
|--------|------|------|------|
| `status` | `string` | ✅ | 상태 (드롭다운) |
| `handler` | `string` | ❌ | 담당자 (드롭다운) |
| `expectedDeliveryDate` | `Date` | ❌ | 예상납기일 (날짜 선택) |
| `handlerRemarks` | `string` | ❌ | 담당자 비고 (텍스트 영역) |

### 🔧 주요 기능

1. **상태 변경**
   ```typescript
   PATCH /api/admin/requests/:requestNo
   Body: {
     status: '접수중' | '접수완료' | '발주지연' | '발주완료' | '처리완료' | '접수취소';
   }
   ```

2. **담당자 배정**
   ```typescript
   PATCH /api/admin/requests/:requestNo
   Body: {
     handlerId: string; // 사용자 ID
   }
   ```

3. **발주 정보 입력**
   ```typescript
   PATCH /api/admin/requests/:requestNo
   Body: {
     orderDate: Date;
     expectedDeliveryDate: Date;
   }
   ```

4. **비고 작성**
   ```typescript
   PATCH /api/admin/requests/:requestNo
   Body: {
     handlerRemarks: string;
   }
   ```

5. **강제 취소**
   - "강제 취소" 버튼
   - 취소 사유 입력 모달
   ```typescript
   POST /api/admin/requests/:requestNo/cancel
   Body: {
     reason: string;
   }
   ```

6. **저장**
   - "저장" 버튼 클릭 시 모든 변경사항 일괄 저장
   - 성공 모달 표시
   - "목록으로" 버튼

### 🎨 UI 컴포넌트
- 신청 정보 카드 (읽기 전용)
- 관리자 액션 폼
  - 상태 선택 드롭다운
  - 담당자 선택 드롭다운
  - 예상납기일 날짜 선택
  - 담당자 비고 텍스트 영역
- 저장 버튼
- 강제 취소 버튼
- 목록으로 버튼

---

## 10. 내 정보 페이지

### 📍 경로
- **URL**: `/profile`
- **권한**: 로그인 사용자
- **리다이렉션**: 비로그인 → `/login`

### 📊 필수 데이터

#### 표시 데이터 (읽기 전용)
| 필드명 | 타입 | 설명 | API |
|--------|------|------|-----|
| `user.userId` | `string` | 사용자 ID | `GET /api/auth/me` |
| `user.name` | `string` | 이름 | `GET /api/auth/me` |
| `user.employeeCode` | `string` | 기사코드 | `GET /api/auth/me` |
| `user.team` | `string` | 소속팀 | `GET /api/auth/me` |
| `user.region` | `string` | 지역 | `GET /api/auth/me` |
| `user.role` | `string` | 역할 | `GET /api/auth/me` |

#### 입력 필드 (비밀번호 변경)
| 필드명 | 타입 | 필수 | 유효성 검증 | 설명 |
|--------|------|------|------------|------|
| `currentPassword` | `string` | ✅ | - 최소 1자 | 현재 비밀번호 |
| `newPassword` | `string` | ✅ | - 최소 6자<br>- 최대 50자 | 새 비밀번호 |
| `confirmPassword` | `string` | ✅ | - `newPassword`와 일치 | 새 비밀번호 확인 |

### 🔧 주요 기능

1. **사용자 정보 표시**
   - 기본 정보 카드 (읽기 전용)

2. **비밀번호 변경**
   ```typescript
   POST /api/auth/change-password
   Body: {
     currentPassword: string;
     newPassword: string;
   }
   Response: {
     success: boolean;
     message?: string;
   }
   ```

3. **유효성 검증**
   - 현재 비밀번호 확인
   - 새 비밀번호 길이 검증 (최소 6자)
   - 새 비밀번호 확인 일치 검증

4. **네비게이션**
   - "뒤로" 버튼 → `/dashboard`

### 🎨 UI 컴포넌트
- 기본 정보 카드
- 비밀번호 변경 폼
- 뒤로 버튼

---

## 📊 공통 데이터 구조

### User 인터페이스
```typescript
interface User {
  id: string;
  userId: string; // 로그인 ID
  password: string; // 해시된 비밀번호
  name: string;
  employeeCode?: string; // 기사코드
  team: string;
  region: string;
  role: '관리자' | '신청자';
  createdAt: Date;
  updatedAt: Date;
}
```

### Request 인터페이스
```typescript
interface Request {
  id: string;
  requestNo: string; // 형식: YYMMDD0001
  requestDate: Date;
  requesterId: string; // User ID
  requesterName: string;
  requesterEmail: string;
  team: string;
  region: string;
  itemName: string;
  modelName?: string;
  serialNo?: string;
  quantity: number;
  assetNo: string;
  deliveryPlace: string;
  phone?: string;
  company?: string;
  remarks?: string;
  photoUrl?: string;
  status: '접수중' | '접수완료' | '발주지연' | '발주완료' | '처리완료' | '접수취소';
  handlerId?: string;
  handler?: string; // 담당자 이름
  handlerRemarks?: string;
  orderDate?: Date;
  expectedDeliveryDate?: Date;
  receiptDate?: Date;
  createdAt: Date;
  updatedAt: Date;
  lastModifiedBy?: string;
}
```

---

## 🔐 권한 체크

### 역할별 접근 권한
| 페이지 | 관리자 | 신청자 |
|--------|--------|--------|
| 로그인 | ✅ | ✅ |
| 사용자 대시보드 | ❌ | ✅ |
| 관리자 대시보드 | ✅ | ❌ |
| 신청 등록 | ❌ | ✅ |
| 내 신청 목록 | ❌ | ✅ |
| 전체 신청 목록 | ✅ | ❌ |
| 통계 및 리포트 | ✅ | ❌ |
| 신청 상세 (본인) | ❌ | ✅ |
| 신청 상세 (타인) | ✅ | ❌ |
| 신청 상세 관리 | ✅ | ❌ |
| 내 정보 | ✅ | ✅ |

---

## 🎨 공통 UI 컴포넌트

### 1. **로딩 스피너**
```tsx
<Spinner />
```

### 2. **토스트 알림**
```tsx
showToast(message: string, type: 'success' | 'error' | 'warning' | 'info');
```

### 3. **모달**
```tsx
<Modal isOpen={isOpen} onClose={onClose}>
  {children}
</Modal>
```

### 4. **상태 뱃지**
```tsx
<Badge status="접수중" /> // 파란색
<Badge status="진행중" /> // 노란색
<Badge status="완료" />   // 초록색
<Badge status="취소" />   // 회색
```

### 5. **날짜 포맷터**
```typescript
formatDate(date: Date): string; // "2026. 1. 7"
formatDateTime(date: Date): string; // "2026. 1. 7 오후 4:58:53"
```

---

## ⚡ 성능 요구사항

### 페이지 로드 시간 목표
- 로그인: < 500ms
- 대시보드: < 1000ms
- 목록 페이지: < 1200ms
- 상세 페이지: < 800ms
- 신청 등록: < 300ms (초기 로드)

### API 응답 시간 목표
- 인증 API: < 200ms
- 조회 API: < 500ms
- 생성 API: < 1500ms (파일 업로드 포함)
- 수정 API: < 800ms

---

**최종 업데이트**: 2026-01-08  
**버전**: v1.0  
**상태**: ✅ Complete




